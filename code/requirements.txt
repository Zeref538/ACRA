# ACRA backend — FastAPI + YOLO (ONNX) CVD re-encoding pipeline
# Install:  python -m pip install -r requirements.txt

# Web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Image / numeric
numpy>=1.26,<3.0
Pillow>=10.0

# Model inference (ultralytics loads the .onnx via onnxruntime)
ultralytics>=8.2
onnxruntime>=1.18

# Auth + config
PyJWT>=2.8
python-dotenv>=1.0
